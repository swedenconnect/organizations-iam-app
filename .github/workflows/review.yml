# Builds and verifies the whole project on every pull request.
#
# Nothing is published and no registry login is needed, so the workflow runs without secrets.
name: Automated review

on:
  pull_request:
    types: [ opened, synchronize, reopened ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-java@v6
        with:
          distribution: 'temurin'
          java-version: '25'
          cache: 'maven'

      - run: mvn clean verify --no-transfer-progress
